{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@vueuse/core": "^9.6.0",
    "@vueuse/nuxt": "^9.6.0",
    "node-sass": "^8.0.0",
    "sass": "^1.56.1",
    "sass-loader": "^13.2.0"
  }
}
